# 本番環境への移行手順（record-fishing-practice → record-fishing）

## 📋 現在の状況

- **練習環境**: `record-fishing-practice` (Ver.1.1.0)
- **本番環境**: `record-fishing` (旧バージョン)
- **バックアップ**: `record-fishing-original` (元の本番環境)

---

## 🎯 移行の目的

Ver.1.1.0の新機能を本番環境（record-fishing）に適用する

### 🆕 Ver.1.1.0 の主要機能
- あたり数（HIT COUNT）記録
- ボーズ（釣果0件）記録
- サイズ分類拡張（30-39cm、<30cm、不明）
- AREA管理タブ
- BAIT管理タブ
- バリデーション強化
- 月別グラフ6段階表示

---

## ⚠️ 移行前の重要確認事項

### 1. データベース状態の確認

#### 練習環境（record-fishing-practice）
- Supabase プロジェクト: 練習用
- データ: テストデータ

#### 本番環境（record-fishing）
- Supabase プロジェクト: 本番用
- データ: **実際の釣果データ（重要）**

### 2. 必須作業

✅ **本番データのバックアップ（必須）**
✅ **Supabase マイグレーションの実行**
✅ **コードのデプロイ**
✅ **動作確認**

---

## 📝 移行手順（ステップバイステップ）

### ステップ1: 本番データのバックアップ 🔴 重要！

#### 1-1. 本番環境のログをCSV出力

1. 本番アプリにアクセス
   ```
   https://record-fishing.pages.dev（または本番URL）
   ```

2. ログイン

3. DATA タブを開く

4. フィルターで全期間を選択

5. **EXPORT CSV** をクリックしてダウンロード

6. ダウンロードしたCSVを安全な場所に保存
   ```
   例: fishing_logs_backup_2026-01-31.csv
   ```

#### 1-2. Supabase データベースの直接バックアップ（推奨）

1. Supabase ダッシュボードにログイン
   ```
   https://supabase.com/dashboard
   ```

2. 本番プロジェクトを選択

3. **Database** → **Backups** を開く

4. **Create Backup** をクリック（手動バックアップ）

5. バックアップ完了を確認

---

### ステップ2: 本番環境のSupabaseマイグレーション

#### 2-1. 必要なマイグレーションSQL

本番環境のSupabaseで以下のSQLを実行します。

##### SQL 1: あたり数フィールドの追加

```sql
-- あたり数フィールドを追加
ALTER TABLE logs ADD COLUMN IF NOT EXISTS hit_count INTEGER;

-- コメントを追加
COMMENT ON COLUMN logs.hit_count IS 'あたりがあった回数（任意）';
```

##### SQL 2: サイズ分類フィールドの追加

```sql
-- 30-39cm フィールドを追加
ALTER TABLE logs ADD COLUMN IF NOT EXISTS size_30 INTEGER DEFAULT 0;

-- <30cm フィールドを追加
ALTER TABLE logs ADD COLUMN IF NOT EXISTS size_under_30 INTEGER DEFAULT 0;

-- コメントを追加
COMMENT ON COLUMN logs.size_30 IS '30-39cm のサイズ';
COMMENT ON COLUMN logs.size_under_30 IS '30cm未満のサイズ';
```

##### SQL 3: baits テーブルの作成

```sql
-- baits テーブルを作成
CREATE TABLE IF NOT EXISTS baits (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    auth_user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    name TEXT NOT NULL,
    is_default BOOLEAN DEFAULT false,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT now(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT now()
);

-- インデックスを作成
CREATE INDEX IF NOT EXISTS idx_baits_auth_user_id ON baits(auth_user_id);
CREATE INDEX IF NOT EXISTS idx_baits_name ON baits(name);

-- RLS（Row Level Security）を有効化
ALTER TABLE baits ENABLE ROW LEVEL SECURITY;

-- ポリシーを作成（ユーザーは自分のデータのみ閲覧・編集可能）
CREATE POLICY "Users can view their own baits"
    ON baits FOR SELECT
    USING (auth.uid() = auth_user_id);

CREATE POLICY "Users can insert their own baits"
    ON baits FOR INSERT
    WITH CHECK (auth.uid() = auth_user_id);

CREATE POLICY "Users can update their own baits"
    ON baits FOR UPDATE
    USING (auth.uid() = auth_user_id);

CREATE POLICY "Users can delete their own baits"
    ON baits FOR DELETE
    USING (auth.uid() = auth_user_id);
```

##### SQL 4: デフォルト餌の初期データ挿入

```sql
-- デフォルト餌を挿入（全ユーザー共通）
-- 注意: auth_user_id は実際のユーザーIDに置き換える必要があります
-- または、アプリ側で初回ログイン時に自動挿入する設計にする

-- 例: システム管理者のユーザーIDで挿入
INSERT INTO baits (auth_user_id, name, is_default) VALUES
    ('00000000-0000-0000-0000-000000000000', 'イガイ', true),
    ('00000000-0000-0000-0000-000000000000', 'ミジ貝', true),
    ('00000000-0000-0000-0000-000000000000', 'フジツボ', true),
    ('00000000-0000-0000-0000-000000000000', 'パイプ', true),
    ('00000000-0000-0000-0000-000000000000', 'カニ', true),
    ('00000000-0000-0000-0000-000000000000', '赤イソメ', true),
    ('00000000-0000-0000-0000-000000000000', '青コガネ', true)
ON CONFLICT DO NOTHING;
```

**注意**: デフォルト餌の挿入方法は、アプリの設計によって変更が必要です。
- 全ユーザー共通のデフォルト餌を使う場合は、システム管理者IDで挿入
- 各ユーザーごとに初期餌を持つ場合は、アプリ側で初回ログイン時に自動挿入

#### 2-2. マイグレーションの実行手順

1. Supabase ダッシュボードにログイン

2. 本番プロジェクトを選択

3. **SQL Editor** を開く

4. 上記のSQLを順番に実行
   - SQL 1: あたり数フィールド
   - SQL 2: サイズ分類フィールド
   - SQL 3: baits テーブル
   - SQL 4: デフォルト餌（任意）

5. 各SQLの実行結果を確認
   - エラーがないことを確認
   - "Success. No rows returned" は正常

---

### ステップ3: 本番環境のコード更新

#### 3-1. Git リポジトリの確認

現在の本番環境（record-fishing）のGitリポジトリを確認します。

```bash
cd /home/user/webapp/record-fishing
git remote -v
git branch
```

#### 3-2. 練習環境のコードを本番にコピー

##### オプション1: Git経由で更新（推奨）

```bash
# 本番環境のディレクトリに移動
cd /home/user/webapp/record-fishing

# 現在の状態をバックアップブランチに保存
git checkout -b backup-before-v1.1.0
git add -A
git commit -m "backup: Save current state before v1.1.0 migration"
git push origin backup-before-v1.1.0

# メインブランチに戻る
git checkout main

# 練習環境の変更を取り込む
# 方法A: 練習環境をリモートに追加して取り込む
git remote add practice ../record-fishing-practice
git fetch practice
git merge practice/main -m "feat: Merge v1.1.0 from practice environment"

# コンフリクトが発生した場合は解決する
# git status で確認
# 手動でファイルを編集してコンフリクトを解決
# git add <resolved-files>
# git commit

# リモートにプッシュ
git push origin main
```

##### オプション2: ファイル直接コピー（簡単だが非推奨）

```bash
# 本番環境を別名でバックアップ
cd /home/user/webapp
cp -r record-fishing record-fishing-backup-$(date +%Y%m%d)

# 練習環境のファイルを本番にコピー
cd /home/user/webapp/record-fishing-practice

# 主要ファイルをコピー
cp index.html /home/user/webapp/record-fishing/
cp -r src /home/user/webapp/record-fishing/
cp package.json /home/user/webapp/record-fishing/
cp vite.config.ts /home/user/webapp/record-fishing/

# ドキュメントもコピー（任意）
cp *.md /home/user/webapp/record-fishing/

# 本番環境でビルド
cd /home/user/webapp/record-fishing
npm install
npm run build

# Git にコミット
git add -A
git commit -m "feat: Update to v1.1.0 with new features"
git push origin main
```

#### 3-3. 環境変数の確認

本番環境の Supabase 接続情報が正しいか確認します。

```javascript
// src/main.ts の確認
const SUPABASE_URL = 'https://your-production-project.supabase.co'
const SUPABASE_ANON_KEY = 'your-production-anon-key'
```

**重要**: 練習環境のSupabase接続情報を本番用に変更する必要があります！

---

### ステップ4: Cloudflare Pages のデプロイ

#### 4-1. Cloudflare Pages の設定確認

1. Cloudflare ダッシュボードにログイン
   ```
   https://dash.cloudflare.com/
   ```

2. **Pages** を選択

3. 本番プロジェクト（record-fishing）を選択

4. **Settings** → **Builds & deployments** を確認
   - Build command: `npm run build`
   - Build output directory: `dist`
   - Root directory: `/`

#### 4-2. デプロイの実行

##### 方法A: Git Push でオートデプロイ（推奨）

```bash
# すでに git push していれば、自動的にデプロイされます
cd /home/user/webapp/record-fishing
git push origin main

# Cloudflare Pages が自動的にビルド＆デプロイを開始
# デプロイ状況は Cloudflare ダッシュボードで確認
```

##### 方法B: 手動デプロイ

1. Cloudflare ダッシュボード → Pages → record-fishing

2. **Create deployment** をクリック

3. Git ブランチを選択（main）

4. **Save and Deploy** をクリック

#### 4-3. デプロイ状況の確認

```
Cloudflare Pages デプロイ状況:
https://dash.cloudflare.com/[your-account-id]/pages/view/record-fishing
```

デプロイが完了するまで約2〜3分待ちます。

---

### ステップ5: 本番環境の動作確認

#### 5-1. 基本動作確認

1. **本番アプリにアクセス**
   ```
   https://record-fishing.pages.dev
   ```

2. **ブラウザのキャッシュをクリア**
   - Chrome: Ctrl+Shift+Delete → キャッシュをクリア
   - または、Ctrl+Shift+R で強制リロード

3. **バージョン確認**
   - 右上に `Ver.1.1.0` が表示されることを確認

#### 5-2. 新機能の動作確認

##### LOGタブ
- [ ] あたり数（HIT COUNT）フィールドが表示される
- [ ] 釣果数を0に設定して記録できる（ボーズ記録）
- [ ] サイズ内訳で30-39cm、<30cmが選択できる
- [ ] バリデーションが動作する（あたり数 ≥ 釣果数）

##### DATAタブ
- [ ] ログ一覧で「ボーズ」が表示される
- [ ] ログ一覧で「あたり: X回」が表示される
- [ ] 月別グラフが6段階で表示される
- [ ] 既存のログが正しく表示される（データ損失なし）

##### AREAタブ
- [ ] AREA タブが表示される
- [ ] エリア・場所・ポイントの追加ができる
- [ ] 編集・削除ができる

##### BAITタブ
- [ ] BAIT タブが表示される
- [ ] 餌の追加ができる
- [ ] 編集・削除ができる
- [ ] LOGタブのドロップダウンに反映される

#### 5-3. 既存データの確認

- [ ] 既存の釣果ログが正しく表示される
- [ ] 既存のログを編集できる
- [ ] CSV出力が正常に動作する
- [ ] グラフが正しく表示される

#### 5-4. エラーチェック

- [ ] ブラウザのコンソールエラーを確認（F12 → Console）
- [ ] ネットワークエラーを確認（F12 → Network）
- [ ] Supabase 接続エラーがないか確認

---

### ステップ6: トラブルシューティング

#### 問題1: バージョンが Ver.1.1.0 にならない

**原因**: ブラウザのキャッシュ

**解決策**:
```
1. ブラウザのキャッシュをクリア
2. Ctrl+Shift+R で強制リロード
3. シークレットモード（プライベートブラウジング）で確認
```

#### 問題2: 新機能が表示されない

**原因**: デプロイが完了していない、またはコードの問題

**解決策**:
```
1. Cloudflare Pages のデプロイ状況を確認
2. デプロイログでエラーがないか確認
3. ビルドが成功しているか確認
```

#### 問題3: Supabase 接続エラー

**原因**: 環境変数が練習用のまま

**解決策**:
```javascript
// src/main.ts を確認
const SUPABASE_URL = 'https://your-production-project.supabase.co'
const SUPABASE_ANON_KEY = 'your-production-anon-key'

// 本番用の値に変更して再デプロイ
```

#### 問題4: 既存データが表示されない

**原因**: マイグレーションの問題、またはRLS設定の問題

**解決策**:
```
1. Supabase ダッシュボードで logs テーブルを確認
2. hit_count、size_30、size_under_30 カラムが存在するか確認
3. RLS ポリシーが正しく設定されているか確認
```

#### 問題5: baits テーブルが見つからない

**原因**: マイグレーションSQL 3 が実行されていない

**解決策**:
```
1. Supabase ダッシュボードで baits テーブルを確認
2. 存在しない場合は、SQL 3 を実行
3. RLS ポリシーが設定されているか確認
```

---

## 📋 移行チェックリスト

### 移行前
- [ ] 本番データをCSVバックアップ
- [ ] Supabase データベースをバックアップ
- [ ] 本番環境のGitをバックアップブランチに保存
- [ ] 練習環境で最終動作確認

### マイグレーション実行
- [ ] Supabase SQL 1 実行（hit_count）
- [ ] Supabase SQL 2 実行（size_30、size_under_30）
- [ ] Supabase SQL 3 実行（baits テーブル）
- [ ] Supabase SQL 4 実行（デフォルト餌、任意）
- [ ] マイグレーションエラーがないことを確認

### コード更新
- [ ] 本番環境にコードをコピー/マージ
- [ ] package.json のバージョンを1.1.0に更新
- [ ] vite.config.ts のバージョンを1.1.0に更新
- [ ] Supabase 接続情報を本番用に変更
- [ ] npm install 実行
- [ ] npm run build 成功を確認
- [ ] Git commit & push

### デプロイ
- [ ] Cloudflare Pages デプロイ開始
- [ ] デプロイ完了を確認（約2〜3分）
- [ ] デプロイログでエラーがないことを確認

### 動作確認
- [ ] 本番アプリにアクセス
- [ ] バージョン Ver.1.1.0 を確認
- [ ] LOGタブの新機能確認
- [ ] DATAタブの新機能確認
- [ ] AREAタブの新機能確認
- [ ] BAITタブの新機能確認
- [ ] 既存データの表示確認
- [ ] 既存データの編集確認
- [ ] CSV出力確認
- [ ] ブラウザコンソールエラー確認

### 最終確認
- [ ] 複数のブラウザで動作確認（Chrome、Safari、Firefox）
- [ ] スマホで動作確認（iOS、Android）
- [ ] 本番データに問題がないことを確認
- [ ] ユーザーに移行完了を通知

---

## ⚠️ 重要な注意事項

### 1. データ損失を防ぐために

- **必ずバックアップを取る**: CSV出力 + Supabase バックアップ
- **テストユーザーで先に確認**: 本番データを触る前にテストユーザーで動作確認
- **ロールバック計画**: 問題があった場合は、バックアップから復元できるようにする

### 2. マイグレーションの可逆性

今回のマイグレーションは、既存のカラムに影響を与えず、新しいカラムを追加するだけなので、
既存データが壊れることはありません。

ただし、万が一問題が発生した場合は、以下の手順でロールバックできます:

```sql
-- ロールバック（新しいカラムを削除）
ALTER TABLE logs DROP COLUMN IF EXISTS hit_count;
ALTER TABLE logs DROP COLUMN IF EXISTS size_30;
ALTER TABLE logs DROP COLUMN IF EXISTS size_under_30;
DROP TABLE IF EXISTS baits CASCADE;
```

### 3. ユーザーへの通知

移行前に、以下の内容をユーザーに通知することを推奨します:

```
【お知らせ】
釣果記録アプリ Ver.1.1.0 へのアップデートを実施します。

【アップデート日時】
2026年1月XX日 XX:XX頃

【所要時間】
約5〜10分

【アップデート内容】
- あたり数記録機能の追加
- ボーズ（釣果0件）記録機能の追加
- サイズ分類の拡張（30-39cm、<30cm）
- エリア管理機能の追加
- 餌管理機能の追加
- バリデーション機能の強化

【注意事項】
- 既存のデータは保持されます
- アップデート後、ブラウザのキャッシュをクリアしてください

ご不便をおかけしますが、よろしくお願いいたします。
```

---

## 🚀 推奨移行スケジュール

### Day 1: 準備（今日）

- [ ] 本データをバックアップ
- [ ] マイグレーションSQLを準備
- [ ] 移行手順を確認
- [ ] ユーザーに事前通知

### Day 2: 移行実施（翌日以降）

- [ ] マイグレーション実行（5分）
- [ ] コード更新（10分）
- [ ] デプロイ（5分）
- [ ] 動作確認（10分）
- [ ] 最終確認（10分）

合計所要時間: **約40分**

### Day 3: 監視期間

- [ ] ユーザーからのフィードバック収集
- [ ] エラーログの確認
- [ ] 必要に応じて修正

---

## 📞 サポート

移行中に問題が発生した場合は、以下を確認してください:

1. **Cloudflare Pages デプロイログ**
   ```
   https://dash.cloudflare.com/[your-account-id]/pages/view/record-fishing
   ```

2. **Supabase ログ**
   ```
   https://supabase.com/dashboard/project/[your-project-id]/logs/explorer
   ```

3. **ブラウザコンソールエラー**
   ```
   F12 → Console タブ
   ```

4. **GitHub Issues**（練習環境のリポジトリ）
   ```
   https://github.com/laurel-otoshikomi/record-fishing-practice/issues
   ```

---

**作成日**: 2026-01-31  
**バージョン**: 1.1.0  
**ステータス**: ✅ 移行手順書完成

このドキュメントに従って、安全に本番環境へ移行してください！ 🎣🚀
